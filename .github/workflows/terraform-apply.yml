name: Terraform Apply

on:
  workflow_call:
    inputs:
      plan_artifact:
        type: string
        required: true
        description: "Name of the plan artifact to apply"
      trigger_deploy:
        type: boolean
        required: false
        default: true
        description: "Whether to check and trigger ECR push if no image exists"
  workflow_dispatch:
    inputs:
      plan_artifact:
        type: string
        required: true
        description: "Name of the plan artifact to apply"
      trigger_deploy:
        type: boolean
        required: false
        default: true
        description: "Whether to check and trigger ECR push if no image exists"

jobs:
  apply:
    runs-on: ubuntu-latest
    environment: production   # requires approval
    permissions:
      id-token: write
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.13.5"

      - name: AWS OIDC Auth
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::368339042148:role/asp_proj-terraform-deploy
          aws-region: ap-southeast-1

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.plan_artifact }}
          path: terraform/

      - name: Terraform Init
        working-directory: terraform
        run: terraform init -input=false

      - name: Terraform Apply
        working-directory: terraform
        run: terraform apply -input=false tfplan

      - name: Notify Success
        if: success()
        run: echo "Terraform apply completed successfully!"

      - name: Notify Failure
        if: failure()
        run: |
          echo "Terraform apply failed!"
          exit 1

  check-and-deploy:
    needs: apply
    if: inputs.trigger_deploy == true
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      needs_image: ${{ steps.check-image.outputs.needs_image }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: AWS OIDC Auth
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::368339042148:role/github_deploy
          aws-region: ap-southeast-1

      - name: Get ECR repository and check for images
        id: check-image
        run: |
          ECR_REPO=$(aws ecr describe-repositories --query 'repositories[0].repositoryUri' --output text)
          REPO_NAME=$(echo $ECR_REPO | awk -F'/' '{print $NF}')
          
          echo "ECR Repository: $ECR_REPO"
          echo "Repository Name: $REPO_NAME"
          
          # Check if any images exist in the repository
          IMAGE_COUNT=$(aws ecr list-images --repository-name $REPO_NAME --query 'length(imageIds)' --output text)
          
          echo "Number of images found: $IMAGE_COUNT"
          
          if [ "$IMAGE_COUNT" = "0" ]; then
            echo "No images found in ECR repository. Will trigger deployment."
            echo "needs_image=true" >> $GITHUB_OUTPUT
          else
            echo "Images already exist in ECR repository. Skipping deployment."
            echo "needs_image=false" >> $GITHUB_OUTPUT
          fi

  deploy:
    needs: check-and-deploy
    if: needs.check-and-deploy.outputs.needs_image == 'true'
    uses: ./.github/workflows/ecr-push.yml
    permissions:
      id-token: write
      contents: read
